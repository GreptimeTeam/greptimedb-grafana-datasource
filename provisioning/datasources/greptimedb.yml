# Configuration file version
apiVersion: 1

# List of data sources to delete from the database.
deleteDatasources:
  - name: GreptimeDB
    orgId: 1

datasources:
  - name: GreptimeDB
    type: grafana-greptime-datasource
    access: proxy
    url: http://localhost:4000
    orgId: 1
    jsonData:
      host: http://localhost:4000
      database: "public"
      username: "default"
      port: 9000
      protocol: native
    secureJsonData:
      # password: ""
